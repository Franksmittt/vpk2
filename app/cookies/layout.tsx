import type { ReactNode } from "react";

type CookiesSegmentLayoutProps = {
  children: ReactNode;
};

const CookiesSegmentLayout = ({ children }: CookiesSegmentLayoutProps) => children;

export default CookiesSegmentLayout;
