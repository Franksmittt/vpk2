import type { ReactNode } from "react";

type ContactSegmentLayoutProps = {
  children: ReactNode;
};

const ContactSegmentLayout = ({ children }: ContactSegmentLayoutProps) => children;

export default ContactSegmentLayout;
