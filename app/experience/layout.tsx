import type { ReactNode } from "react";

type ExperienceSegmentLayoutProps = {
  children: ReactNode;
};

const ExperienceSegmentLayout = ({ children }: ExperienceSegmentLayoutProps) => children;

export default ExperienceSegmentLayout;
