import type { ReactNode } from "react";

type TermsSegmentLayoutProps = {
  children: ReactNode;
};

const TermsSegmentLayout = ({ children }: TermsSegmentLayoutProps) => children;

export default TermsSegmentLayout;
