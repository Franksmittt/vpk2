import type { ReactNode } from "react";

type LodgeSegmentLayoutProps = {
  children: ReactNode;
};

const LodgeSegmentLayout = ({ children }: LodgeSegmentLayoutProps) => children;

export default LodgeSegmentLayout;
