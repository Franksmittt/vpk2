import type { ReactNode } from "react";

type SpeciesSlugSegmentLayoutProps = {
  children: ReactNode;
};

const SpeciesSlugSegmentLayout = ({ children }: SpeciesSlugSegmentLayoutProps) => children;

export default SpeciesSlugSegmentLayout;
